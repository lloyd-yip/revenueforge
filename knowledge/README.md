# Knowledge Base

Tagged markdown files recording how this project actually behaves — surprises, constraints and
discoveries that would otherwise be rediscovered every session.

Write 10–30 line files with front-matter `tags:`. Sessions load files whose tags match the task at
hand. Promote new behavioural discoveries here during `/handoff`.
